# data/

Dataset generation scripts and versioned dataset outputs go here.

- Generation scripts (calls to the teacher/frontier model, filtering logic)
- Dataset snapshots (e.g. `v1/`, `v2/`) so iteration is diffable
- The final published dataset is the deliverable — this is where it's built before going to HF Hub
